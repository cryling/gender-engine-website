---
import { Image } from "astro:assets";
import cyanBlur from "../assets/blur-cyan.png";

interface Props {
  repoHref: string;
}

const { repoHref } = Astro.props;
---

<div class="relative z-10 md:text-center lg:text-left">
  <Image
    src={cyanBlur}
    alt=""
    height={530}
    width={530}
    style="color:transparent"
    class="absolute bottom-full right-full -mb-56 -mr-72 opacity-50"
  />

  <div class="relative">
    <h1
      class="inline bg-gradient-to-r from-teal-400 via-emerald-400 to-teal-400 dark:from-teal-200 dark:via-emerald-400 dark:to-teal-200 bg-clip-text text-5xl font-bold tracking-tight text-transparent"
    >
      Find the Gender of any Name
    </h1>
    <p class="mt-4 text-lg leading-relaxed text-neutral-600 dark:text-neutral-400">
      A simple, no-fuss, free API to determine the gender of any name. Self-hosted and open-source.
    </p>
    <div class="mt-8 flex gap-4 md:justify-center lg:justify-start">
      <a
        class="rounded-full bg-emerald-500 py-2.5 px-5 text-sm font-semibold text-white shadow-sm hover:bg-emerald-600 focus:outline-none focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-emerald-500 active:bg-emerald-700 transition-colors"
        href="/docs"
      >
        Get started
      </a>
      <a
        class="rounded-full py-2.5 px-5 text-sm font-medium text-neutral-700 dark:text-neutral-300 ring-1 ring-neutral-200 dark:ring-neutral-700 hover:bg-neutral-50 dark:hover:bg-neutral-800 focus:outline-none focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-white/50 transition-colors"
        href={repoHref}
      >
        View on GitHub
      </a>
    </div>
  </div>
</div>
